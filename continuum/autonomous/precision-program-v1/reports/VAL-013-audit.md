# VAL-013 shape-heuristic audit

Scope: static audit of `crates/tldr-core/src/` as-is, including the uncommitted VAL-012 policy swap in `builder_v2.rs` and `bin/callgraph_resolution_stats.rs`. I read each listed function and at least one caller before classification.

## Findings

| # | file:line | predicate | reachable langs | classification | evidence (guard line / caller trace) | fix sketch (BUG-CLASS only) |
|---:|---|---|---|---|---|---|
| 1 | `crates/tldr-core/src/callgraph/resolution.rs:757` | `target.contains("__import__") || target.contains("importlib")` | All `resolve_call` callers; `builder_v2.rs:451/536/538/617/627` routes non-receiver calls here for the configured language. | BUG-CLASS | Python dynamic import tokens are checked before the TS/JS `target == "import"` language guard at `resolution.rs:761`. A non-Python project symbol containing `importlib` or `__import__` is forced unresolved. | Add a `LanguagePolicy` dynamic-import token/guard or restrict this branch to Python-family behavior; leave the TS/JS `import` guard explicit. |
| 2 | `crates/tldr-core/src/callgraph/module_index.rs:384` | Generic `simple_module_name(module)` alias before `match self.language`; helper splits on `.`, `/`, and `\` at `module_index.rs:1755`. | All `ModuleIndex::build*` languages via `index_file` at `module_index.rs:324-351`; `ImportResolver` then uses `lookup`/`reverse_lookup`. | BUG-CLASS | The simple alias is registered before the language-specific arms at `module_index.rs:390-559`, so languages whose policy should not expose bare suffix aliases still get one in the module index. | Gate the generic simple alias with `policy_for(lang).module_alias_style == AliasStyle::DottedSuffix`; keep the existing language-specific aliases inside the match arms. |
| 3 | `crates/tldr-core/src/callgraph/builder_v2.rs:178` | `module.split('.').next_back()` simple-module alias in `build_indices_parallel`; method twin at `builder_v2.rs:199`. | Public helper accepts any `language: &str`; main builder calls it at `builder_v2.rs:756-757` but discards these returned indices, while external/test callers can consume them. | BUG-CLASS | Unlike the main index loop fixed by VAL-012 at `builder_v2.rs:848-854`, this earlier helper does not call `module_uses_dotted_alias`. | Compute `dotted_alias` from `LanguagePolicy` in `build_indices_parallel` and guard both simple function and `Class.method` alias insertions. |
| 4 | `crates/tldr-core/src/callgraph/resolution.rs:826` | Direct import-map fallback computes `simple_module = module_path.split('.').next_back()` and probes it at `resolution.rs:884-892`. | All `CallType::Direct` calls in `resolve_call`; import maps are built for every configured language in `builder_v2.rs:1052-1064`. | BUG-CLASS | The fallback is not gated by `language` or policy and runs before the full `module_path` fallback at `resolution.rs:894-903`. Disambiguation protects collisions but does not decide whether the language permits a dotted suffix alias. | Guard the simple-module probe with `AliasStyle::DottedSuffix`; preserve exact, extension-stripped, bare relative, and full-module probes. |
| 5 | `crates/tldr-core/src/callgraph/resolution.rs:2224` | Receiver module import fallback computes `simple_module = module_path.split('.').next_back()` and probes it at `resolution.rs:2238-2247`. | All `Method`/`Attr` receiver calls with `ModuleImports`, via `resolve_call_with_receiver_enclosing` at `resolution.rs:1910-1917`. | BUG-CLASS | The full module key is tried first at `resolution.rs:2229-2236`, then the unpolicy-gated bare dotted suffix fallback runs for every language. This is the same shape as the W2-29 path-B fallback, now in a shared resolver. | Gate the simple-module probe with `AliasStyle::DottedSuffix`; unknown language should behave as no alias. |
| 6 | `crates/tldr-core/src/callgraph/resolution.rs:2357` | `capitalize_first(receiver)` guesses a class receiver from a lowercase receiver. | All `Method`/`Attr` receiver calls after exact module/class/local receiver resolution misses; caller is `resolution.rs:1972-1975`. | BUG-CLASS | `language` is only passed into `resolve_method_in_class_or_bases` after the capitalization guess; it does not decide whether the guess is valid. This can resolve `foo.bar()` to `Foo.bar()` across every language. | Add a policy field for receiver case fallback, or replace with AST-derived receiver type/import evidence. |
| 7 | `crates/tldr-core/src/callgraph/resolver.rs:61` | Legacy `ModuleResolver::index_file` registers `module_name.split('.').next_back()` for every configured language. | Public `ModuleResolver` is exported from `callgraph/mod.rs`; `path_to_module` is language-aware at `resolver.rs:80-157`, then the alias is unconditional. | BUG-CLASS | TS/JS, Java, Python, and other dotted/path module spellings all pass through the same simple-alias insertion with no policy check. | Route `ModuleResolver` through `LanguagePolicy` or remove parity with the modern policy-gated builder path. |
| 8 | `crates/tldr-core/src/callgraph/resolver.rs:204` | Legacy `resolve_function` falls back to `import.module.split('.').next_back()` at `resolver.rs:204-210`. | Public `ModuleResolver::resolve_function` for any language configured via `with_language`. | BUG-CLASS | The resolver checks `import.is_from` but not `self.language` before probing the bare dotted suffix. | Gate this probe with `AliasStyle::DottedSuffix`, matching the modern resolver policy. |
| 9 | `crates/tldr-core/src/callgraph/resolution.rs:1410` | `is_builtin_method_name(bare_target)` blocks fuzzy resolution for fixed method names. | All `Method`/`Attr` receiver calls with no `type_filter`, via local fuzzy `resolution.rs:2665-2669` and global fuzzy `resolution.rs:2802-2806`. | BUG-CLASS?-UNCERTAIN | The list contains Python dict/list/string/io names, Go serialization names, and common verbs. It is a deliberate false-positive defense, but all languages consult it and a project method named `items`, `append`, `run`, etc. can be suppressed without a language guard. Need owner decision whether this global blocklist is policy data. | If confirmed, move to `LanguagePolicy` as per-language builtin method vocabulary, with common/global names split explicitly if desired. |
| 10 | `crates/tldr-core/src/callgraph/resolution.rs:1361` | `is_stdlib_type(type_name)` admits Python/Ruby stdlib type names into `receiver_type_filter`. | All `Method`/`Attr` receiver calls with inferred receiver type, via `receiver_type_filter` at `resolution.rs:2616-2624`. | BUG-CLASS?-UNCERTAIN | The list is mostly Python plus Ruby names and is consulted without language. It conservatively prevents fuzzy binding for known stdlib types, but it can affect non-Python/Ruby typed receivers with the same spelling. | If confirmed, move stdlib type names into `LanguagePolicy` or split a documented language-neutral set from per-language sets. |
| 11 | `crates/tldr-core/src/callgraph/import_resolver.rs:229` | Absolute import normalization strips `.js/.jsx/.ts/.tsx/.mjs/.cjs/.rb/.php/.lua/.luau` before the language branch. | All `ImportResolver::resolve_uncached` callers; language is available at `import_resolver.rs:198`. | BUG-CLASS?-UNCERTAIN | Relative/file-path resolution is partitioned below at `import_resolver.rs:247-259` and `268-271`, but extension stripping happens first for all languages. Need confirmation whether extractors can emit cross-language extension-bearing module specs. | Move extension stripping behind a language match or use `language_extensions(language)` at `import_resolver.rs:1216`. |
| 12 | `crates/tldr-core/src/callgraph/resolution.rs:131` | `constructor_method_candidates(language, class_name)` name table. | Constructor resolution via `resolve_constructor_target` at `resolution.rs:146-152`; callers pass the configured language. | PARTITIONED-SAFE | Explicit `match language.to_lowercase().as_str()` arms at `resolution.rs:132-141`; no wildcard constructor names except empty `Vec` for unknown. | |
| 13 | `crates/tldr-core/src/callgraph/resolution.rs:2117` | `is_colon_receiver(target, receiver, bare_target, language)` checks Lua/Luau colon spelling. | Receiver resolution via `resolution.rs:1774-1784`. | PARTITIONED-SAFE | Explicit `matches!(language, "luau" | "lua")` guard at `resolution.rs:2118`; non-Lua languages cannot enter the colon-call path. | |
| 14 | `crates/tldr-core/src/analysis/importers.rs:199` | `module_matches(import_module, target, language)` prefix/suffix/basename matching. | Importer CLI/query path calls `module_matches` from `find_import_in_file` at `importers.rs:129`. | PARTITIONED-SAFE | Every shape rule is inside an explicit `Language` match arm: Python `201-226`, TS/JS `227-260`, Go `261-264`, JVM/C# `301-329`, C/C++ `330-349`, PHP `364-389`, Solidity/Lua/Ruby `406-439`, Rust `444-461`, Elixir `470`, fallback exact-only `471`. | |
| 15 | `crates/tldr-core/src/callgraph/import_resolver.rs:247` | Relative/path-module checks with `starts_with("./")`, `starts_with("../")`, and path-like `contains('/')`. | `ImportResolver::resolve_uncached` for all import resolution. | PARTITIONED-SAFE | The relative branch immediately matches on `language` at `import_resolver.rs:248-259`; path-like absolute imports are restricted to `ruby/php/lua/c/cpp` at `268-271`. | |
| 16 | `crates/tldr-core/src/callgraph/imports.rs:260` | Go import alias and suffix matching with `module_path.ends_with(format!("/{}", known))`. | Called from the main builder only when `config.language == "go"` at `builder_v2.rs:1058-1059`. | PARTITIONED-SAFE | The helper itself is Go-specific by contract and caller guard; tests call it directly as Go import augmentation. | |
| 17 | `crates/tldr-core/src/callgraph/builder_v2.rs:848` and `crates/tldr-core/src/bin/callgraph_resolution_stats.rs:82` | VAL-012 simple-module alias guarded by `dotted_alias`. | Main callgraph build path and stats bin. | PARTITIONED-SAFE | `dotted_alias` comes from `module_uses_dotted_alias(&config.language)` at `builder_v2.rs:816` and from `module_uses_dotted_alias(&language)` at stats line `64`; unknown languages return false through the VAL-011 `Language::from_str` route. | |
| 18 | `crates/tldr-core/src/callgraph/module_index.rs:390` | Language-specific alias blocks in `compute_module_aliases`. | All `ModuleIndex::build*` paths after generic alias registration. | PARTITIONED-SAFE | The TS/JS, Go, PHP, Ruby, Lua/Luau, C/C++, Swift, Elixir, and OCaml alias rules are each inside `match self.language.as_str()` arms at `390-559`. The generic pre-match alias is covered separately as finding #2. | |
| 19 | `crates/tldr-core/src/callgraph/type_resolver.rs:1289` | Receiver type spelling/name heuristics in Python, TS/JS, Go, and Rust helper functions. | Builder applies type resolution to supported languages and dispatches through `resolve_receiver_type(Language, ...)`. | PARTITIONED-SAFE | `resolve_receiver_type` matches `Language` at `type_resolver.rs:1296-1310`; representative heuristics like TS interface spelling at `624`, Go exported type checks at `727`, and Rust `::` checks at `1044` are inside their language resolver path. | |
| 20 | `crates/tldr-core/src/callgraph/var_types.rs:910` | Builtin lists, `New*`, uppercase, and qualified-name suffix extraction in var-type inference. | Selected by `extract_definitions` language match at `module_path.rs:67-79`. | PARTITIONED-SAFE | Representative extractors are language-specific: Go `var_types.rs:910`, TS `1434`, Java `1781`, Rust `2210`, Kotlin `2578`, PHP `2957`, CSharp `3266`, Swift `3589`, Lua/Luau `4165/4170`, C++ `4450`. | |
| 21 | `crates/tldr-core/src/callgraph/builder_v2.rs:400` | `super` constructor name handling. | Main call-site loop before generic resolution at `builder_v2.rs:300-307`. | PARTITIONED-SAFE | `supports_super_ctor` explicitly matches Java/Kotlin/Scala/Swift/TS/JS/CSharp/Solidity at `builder_v2.rs:400-416`; non-matching languages return `None`. | |
| 22 | `crates/tldr-core/src/callgraph/resolution.rs:2057` | `receiver_is_type_spelling`: paren-free final segment starts uppercase. | All receiver calls; used for strict fuzzy gates at `resolution.rs:1992-1999` and `2007-2008`. | DOCUMENTED-SAFE | Cross-language reach is intentional and conservative: it runs only after exact receiver/module/class resolution fails and only restricts fuzzy fallback for capitalized external/stdlib type spellings, as documented at `resolution.rs:2045-2056`. It does not create a new target. | |
| 23 | `crates/tldr-core/src/callgraph/resolution.rs:1628` | `bare_class_name` strips `::`, `.`, `/`, `\` qualifiers. | Colon/self sibling guard via `resolution.rs:1863-1864`; definer-cardinality helpers also use it for class deduplication. | DOCUMENTED-SAFE | It operates on AST-extracted class names to collapse duplicate qualified copies when counting definers, documented at `resolution.rs:1623-1627`. It is not module import aliasing and does not choose a callee by suffix alone. | |
| 24 | `crates/tldr-core/src/callgraph/languages/base.rs:208` | `determine_call_type` classifies `.` as `Attr` and `::` as `Static`. | Used by language handlers, reached through `LanguageRegistry::get(language)` in `module_path.rs:40-64`. | DOCUMENTED-SAFE | This is syntax classification for already-extracted call targets. It does not resolve across files; the resolver later applies language-specific or typed resolution. | |
| 25 | `crates/tldr-core/src/callgraph/type_aware_resolver.rs:596` | Union type detection by `type_name.contains('|')`. | Type-aware resolver only after receiver type inference in builder. | DOCUMENTED-SAFE | The check tests a structural type-expression property and expands/declines union resolution; it is not a module or symbol suffix alias and does not apply language-specific names cross-language. | |
| 26 | `crates/tldr-core/src/callgraph/builder.rs:146` | Entry-point naming/decorator checks such as `test_`, `__dunder__`, `route`, `fixture`. | Public helper `is_entry_point(func)` outside call resolution; callgraph builder path uses V2 for edge construction. | DOCUMENTED-SAFE | This affects entry-point classification, not cross-file callee resolution or import aliasing. The names are intentionally broad entry-point conventions, not module/symbol lookup keys. | |

## Summary

Total findings: 26

- BUG-CLASS: 8
- BUG-CLASS?-UNCERTAIN: 3
- PARTITIONED-SAFE: 10
- DOCUMENTED-SAFE: 5

Confirmed BUG-CLASS list:

- `resolution.rs:757`: Python dynamic import substring filter runs before language guard.
- `module_index.rs:384`: generic simple module alias registered before language-specific policy.
- `builder_v2.rs:178`: `build_indices_parallel` still inserts bare dotted suffix aliases without policy.
- `resolution.rs:826`: direct import-map simple-module fallback is unpolicy-gated.
- `resolution.rs:2224`: module-import receiver simple-module fallback is unpolicy-gated.
- `resolution.rs:2357`: lowercase receiver is capitalized into a class guess for all languages.
- `resolver.rs:61`: legacy `ModuleResolver::index_file` simple-module alias is unpolicy-gated.
- `resolver.rs:204`: legacy `ModuleResolver::resolve_function` simple-module fallback is unpolicy-gated.

Uncertain bug-class candidates needing owner/policy confirmation:

- `resolution.rs:1410`: global builtin method-name blocklist.
- `resolution.rs:1361`: global stdlib type-name blocklist.
- `import_resolver.rs:229`: all-language extension stripping before language branch.

BUG-CLASS instances found: 8
